interface:
  display_name: "Open PR or MR"
  short_description: "Create or update a review request"
  default_prompt: "Use $pr to push this branch and open or update its review request."
policy:
  allow_implicit_invocation: true
