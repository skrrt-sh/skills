interface:
  display_name: "Commit Changes"
  short_description: "Create focused conventional commits"
  default_prompt: "Use $commit to split and commit the current work into coherent changes."
policy:
  allow_implicit_invocation: true
